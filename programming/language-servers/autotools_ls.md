https://autotools-language-server.readthedocs.io/en/latest/
```
pipx install autotools-language-server
```

# neovim config note
Requires the `util` package? from https://github.com/neovim/nvim-lspconfig plugin